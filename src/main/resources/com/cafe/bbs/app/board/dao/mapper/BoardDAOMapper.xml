<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cafe.bbs.app.board.dao">
	
	<select id="getAllBoardList"
			resultType="com.cafe.bbs.app.board.vo.BoardVO">
		SELECT BOARD_ID
			 , BOARD_NAME
			 , BOARD_URL
		  FROM BOARD
	</select>
	
	<select id="getBoardId"
			resultType="string">
		SELECT BOARD_ID
		  FROM BOARD
		 WHERE BOARD_URL = #{_parameter}
	</select>

	<select id="getBoardUrl"
			resultType="string">
		SELECT BOARD_URL
		  FROM BOARD
		 WHERE BOARD_ID = #{_parameter}
	</select>
	
	<select id="getBoardVO"
			parameterType="string"
			resultType="com.cafe.bbs.app.board.vo.BoardVO">
		SELECT BOARD_ID
			 , BOARD_NAME
			 , BOARD_URL
		  FROM BOARD
		 WHERE 1=1
		   AND BOARD_URL = #{_parameter}
	</select>
</mapper>